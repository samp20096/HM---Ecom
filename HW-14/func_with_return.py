# 2.

# a.
def my_ave(x: int, y: int):
    """Calculating the average of 2 given integers"""
    return (x + y) / 2


# b.
def my_headline(x: str):
    """returning str in upper case and adding ! to the end"""
    return x.upper() + "!"


# c.
def concat_list(x: list[int], y: list[int], z: list[int]):
    """returning 3 given lists as one"""
    return x + y + z
